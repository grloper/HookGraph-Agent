# Runtime — the whole pipeline runs offline on these three pins alone.
langgraph==1.2.9
langchain-core==1.4.9
pydantic==2.13.4

# Optional: Claude-powered creative engine (enable with `python main.py --llm`).
# pip install anthropic
